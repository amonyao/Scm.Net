namespace Com.Scm
{
    public class ScmEnv
    {
        public const string FORMAT_DATE = "yyyy-MM-dd";
        public const string FORMAT_TIME = "HH:mm:ss";
        public readonly string FORMAT_DATETIME = FORMAT_DATE + ' ' + FORMAT_TIME;
    }
}
